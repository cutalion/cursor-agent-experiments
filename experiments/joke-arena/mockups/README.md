# Joke Arena Mockups

Place the future UI mockups in this directory with these exact names:

- `main.png`
- `compare.png`
- `leaderboard.png`

Agents implementing the experiment should use these screenshots as visual references when they are present. Until then, the implementation should provide a clean responsive UI with the same page responsibilities described in `../plan.md`.
